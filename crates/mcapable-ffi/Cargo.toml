[package]
name = "mcapable-ffi"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
bytes = "1.11"
mcapable-core = { path = "../mcapable-core", features = ["parsers"] }
serde_json = "1.0"
